[project]
name = "ai-orchestration-shared"
version = "0.1.0"
description = "Shared services for AI orchestration patterns"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["shared"]
